---
title: "4.3. Hybrid Architectures and the Log Abstraction"
description: "The commit log as the source of truth: Lambda and Kappa architectures, log compaction, and Change Data Capture."
lastUpdated: 2026-07-28
sidebar:
  order: 3
---

The commit log is the abstraction that collapses the batch/stream divide: an append-only, replayable sequence of records that a batch job and a stream processor can both consume. Once you see the log as the source of truth, Lambda architecture (maintaining separate batch and speed layers) reveals itself as accidental complexity, and Kappa (everything is a stream, reprocess by replaying) becomes the simplification. This chapter covers that progression, log compaction, and Change Data Capture as the bridge from databases into the streaming world.

### Topics Covered

* **[4.3.1. The Commit Log Abstraction: The Core Idea Behind Kafka/Pulsar](/distributed-systems/en/data-processing/hybrid-architectures/commit-log-abstraction/)**: Establishes the append-only commit log as the unifying abstraction behind Kafka and Pulsar.
* **[4.3.2. Lambda Architecture: The Complexity of Batch + Speed Layers](/distributed-systems/en/data-processing/hybrid-architectures/lambda-architecture/)**: Analyzes Lambda architecture and the accidental complexity of maintaining separate batch and speed layers.
* **[4.3.3. Kappa Architecture: Everything Is a Stream](/distributed-systems/en/data-processing/hybrid-architectures/kappa-architecture/)**: Explains Kappa architecture, which treats everything as a stream and reprocesses by replaying the log.
* **[4.3.4. Log Compaction: Kafka Compacted Topics and Tombstones](/distributed-systems/en/data-processing/hybrid-architectures/log-compaction/)**: Covers Kafka log compaction, tombstones, and the cleaner thread that retains only the latest value per key.
* **[4.3.5. Change Data Capture (CDC): Real-Time Synchronization](/distributed-systems/en/data-processing/hybrid-architectures/change-data-capture/)**: Explains Change Data Capture, streaming a database's changes in real time from its transaction log.
* **[4.3.6. Debezium: Practical Database CDC Implementation](/distributed-systems/en/data-processing/hybrid-architectures/debezium/)**: Covers Debezium's practical CDC implementation, snapshot-then-stream coordination, and ordering guarantees.
