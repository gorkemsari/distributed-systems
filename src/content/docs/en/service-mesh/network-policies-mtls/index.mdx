---
title: "10.2. Network Policies and mTLS"
description: "Closing the flat-network gap: Kubernetes network policies, mTLS, certificate lifecycle, and eBPF networking."
lastUpdated: 2026-07-28
sidebar:
  order: 2
---

By default, every pod in a cluster can talk to every other pod -- a flat network that turns one compromised service into cluster-wide access. Closing that gap means two things: network policies that act as a pod-level firewall, and mTLS that makes every service prove its identity cryptographically on every call. This chapter covers both, the certificate lifecycle (issuance, rotation, and the expiry that causes classic mesh outages) that makes mTLS operable, and the eBPF and Gateway API developments reshaping how it is enforced.

### Topics Covered

* **[10.2.1. Kubernetes Network Policy: Pod-Level Firewall](/distributed-systems/en/service-mesh/network-policies-mtls/kubernetes-network-policy/)**: Covers Kubernetes Network Policies that act as a pod-level firewall over the default-flat cluster network.
* **[10.2.2. mTLS: Mutual Authentication and Encryption](/distributed-systems/en/service-mesh/network-policies-mtls/mtls/)**: Explains mutual TLS, making every service prove its identity cryptographically on every call.
* **[10.2.3. Certificate Lifecycle: Cert-Manager, SPIFFE / SPIRE](/distributed-systems/en/service-mesh/network-policies-mtls/certificate-lifecycle/)**: Covers certificate issuance and rotation with cert-manager and SPIFFE/SPIRE, and the expiry that causes outages.
* **[10.2.4. eBPF Networking: Kernel-Level CNI with Cilium](/distributed-systems/en/service-mesh/network-policies-mtls/ebpf-networking/)**: Explains Cilium's eBPF-based CNI that enforces networking and policy at the kernel level.
* **[10.2.5. Ingress vs. Gateway API: Next-Generation Traffic Management](/distributed-systems/en/service-mesh/network-policies-mtls/ingress-vs-gateway-api/)**: Compares the legacy Ingress with the role-oriented Gateway API for next-generation traffic management.
